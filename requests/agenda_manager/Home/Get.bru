meta {
  name: Get
  type: http
  seq: 1
}

get {
  url: {{base_url}}/home
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test('Should response 200 when is succeeded', () => {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });
  
  test('Should response hello world when succeeded', () => {
    const data = res.getBody();
    expect(res.body.value).to.equal('Hello world');
  });
}
